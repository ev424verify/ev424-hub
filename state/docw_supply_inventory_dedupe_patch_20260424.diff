--- /home/ec2-user/bin/새문서후보보충.stake_20260424T205439+0900	2026-04-24 18:14:48.333220617 +0900
+++ /home/ec2-user/bin/새문서후보보충	2026-04-24 20:54:39.705599656 +0900
@@ -550,7 +550,33 @@
     base = re.sub(r"(?i)(\.(?:ara|jpn|kor|spa|fra|deu|zho|chi|cht|chs|rus|por|ita))(?=\.pdf$)", "", base)
     return base.lower()
 
+def supply_inventory_has_url(url):
+    inv = Path("/home/ec2-user/ev424_hub_ops/state/docw_supply_inventory_v1.jsonl")
+    if not inv.exists():
+        return False
+    p = urlparse((url or "").strip())
+    url_canon = f"{p.scheme}://{p.netloc}{p.path}"
+    doc_key = normalize_doc_key(url)
+    import json
+    for line in inv.read_text(encoding="utf-8", errors="ignore").splitlines():
+        try:
+            j = json.loads(line)
+        except Exception:
+            continue
+        raw = (j.get("candidate_url") or "").strip()
+        name = (j.get("name") or "").strip()
+        pr = urlparse(raw)
+        raw_canon = f"{pr.scheme}://{pr.netloc}{pr.path}"
+        if raw_canon and raw_canon == url_canon:
+            return True
+        if doc_key and (normalize_doc_key(raw) == doc_key or normalize_doc_key(name) == doc_key):
+            return True
+    return False
+
+
 def registry_has_url(url):
+    if supply_inventory_has_url(url):
+        return True
     if not REG.exists():
         return False
     p = urlparse((url or "").strip())
